# Flask Configuration
FLASK_ENV=development
SECRET_KEY=change-me-to-a-random-string-in-production

# Database (optional - SQLite is default)
# DATABASE_URL=sqlite:///./mental_health_chatbot.db
# For PostgreSQL: DATABASE_URL=postgresql://user:password@localhost:5432/mental_health_db

# ML Models Cache (optional)
# TRANSFORMERS_CACHE=/path/to/cache

# Hugging Face Token (only if using gated models)
# HF_TOKEN=your-huggingface-token
